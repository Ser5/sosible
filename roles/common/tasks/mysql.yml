- name:   Configure MySQL
  become: yes
  tags:   mysql
  ini_file:
    path:    "{{ p.mysql.config_path }}"
    section: mysqld
    option:  "{{ item.key }}"
    value:   "{{ item.value }}"
  loop:
    "{{ p.mysql.config | dict2items }}"
