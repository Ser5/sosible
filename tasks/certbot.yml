- name:   Certbot
  become: yes
  tags:   certbot
  block:
    - name: Install certbot with python 3
      apt:
        name: [python3-pip, python3-certbot-nginx, certbot]
        state: present

    - name: Install certbot-regru
      pip:
        name: certbot-regru

    - name: chmod 0600 /etc/letsencrypt/regru.ini
      file:
        path:  /etc/letsencrypt/regru.ini
        state: touch
        owner: root
        group: root
        mode:  '0600'
